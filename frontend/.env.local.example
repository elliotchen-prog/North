# Optional: set to "false" when backend is running to use real API
# NEXT_PUBLIC_USE_MOCK_API=false

# Backend API base URL (used when NEXT_PUBLIC_USE_MOCK_API=false)
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
